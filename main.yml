---
-name: github clone 
 hosts:all 
 become:true
 vars:
  ansible_secret.yml
tasks:
  repo: https://username:{{password}}@github.com/GurlovleenSingh/Ansible-Vault.git
  dest: Ansible-vault-github/clone
